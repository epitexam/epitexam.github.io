---
export interface Props {
  icon: string;
  name: string;
  description: string;
  level?: "mastered" | "practiced" | "exploring";
  link?: string;
}

const { icon, name, description, level = "practiced", link } = Astro.props;

const CardTag = link ? "a" : "div";
const cardProps = link
  ? {
      href: link,
      target: "_blank",
      rel: "noopener noreferrer",
      "aria-label": `En savoir plus sur ${name} (s'ouvre dans un nouvel onglet)`,
    }
  : {};

const LEVEL_LABELS: Record<string, string> = {
  mastered: "Maîtrisé",
  practiced: "Pratiqué",
  exploring: "En exploration",
};

const levelLabel = LEVEL_LABELS[level] ?? level;
---

<CardTag class="tool-card" data-level={level} {...cardProps}>
  <div class="card-glow" aria-hidden="true"></div>
  <div class="tool-inner">
    <div class="tool-icon-container">
      <span class="tool-icon-text" aria-hidden="true">{icon}</span>
    </div>
    <div class="tool-content">
      <h3 class="tool-title">
        {name}
        {
          link && (
            <span class="external-link-icon" aria-hidden="true">
              ↗
            </span>
          )
        }
      </h3>
      <p class="tool-description">{description}</p>
    </div>
  </div>
  <span class="level-indicator" aria-label={`Niveau : ${levelLabel}`} role="img"
  ></span>
</CardTag>

<style>
  .tool-card {
    position: relative;
    width: 100%;
    height: 100%;
    padding: 1.5rem;
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-soft);
    transition:
      transform var(--duration-base) var(--ease-spring),
      border-color var(--duration-base) var(--ease-spring),
      box-shadow var(--duration-base) var(--ease-spring);
    overflow: hidden;
    isolation: isolate;
    display: flex;
    flex-direction: column;
    color: inherit;
  }

  .tool-card :is(h1, h2, h3, h4, h5, h6) {
    animation: none !important;
  }
  a.tool-card {
    cursor: pointer;
  }
  a.tool-card:focus-visible {
    outline: 2px solid var(--color-accent);
    outline-offset: 2px;
  }

  .tool-card:hover {
    transform: translateY(-2px);
    border-color: rgba(var(--accent), 0.2);
    box-shadow: var(--shadow-strong);
  }

  .card-glow {
    position: absolute;
    inset: 0;
    border-radius: inherit;
    background: radial-gradient(
      ellipse at 50% 0%,
      rgba(var(--accent), 0.08) 0%,
      transparent 60%
    );
    opacity: 0;
    z-index: 0;
    pointer-events: none;
    transition: opacity var(--duration-base) ease;
  }

  .tool-card:hover .card-glow {
    opacity: 1;
  }

  .tool-inner {
    display: flex;
    align-items: flex-start;
    text-align: left;
    gap: 1.25rem;
    z-index: 3;
    position: relative;
  }

  .tool-icon-container {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 2.75rem;
    height: 2.75rem;
    min-width: 2.75rem;
    min-height: 2.75rem;
    background: var(--tint-05);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-sm);
    flex-shrink: 0;
    transition:
      border-color var(--duration-base) ease,
      background var(--duration-base) ease;
    overflow: hidden;
  }

  .tool-icon-text {
    font-size: 1.15rem;
    line-height: 1;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .tool-card[data-level="mastered"] .tool-icon-container {
    border-color: rgba(var(--accent), 0.4);
    background: rgba(var(--accent), 0.08);
  }
  .tool-card[data-level="practiced"] .tool-icon-container {
    border-color: var(--color-border);
    background: var(--tint-05);
  }
  .tool-card[data-level="exploring"] .tool-icon-container {
    border-color: var(--color-border);
    background: var(--color-surface);
    opacity: 0.7;
  }

  .tool-content {
    flex: 1;
    min-width: 0;
  }

  .tool-title {
    margin: 0 0 0.35rem;
    font-family: var(--font-title);
    font-size: 1.05rem;
    font-weight: 600;
    color: var(--color-text);
    letter-spacing: -0.02em;
    line-height: 1.3;
    display: flex;
    align-items: center;
    gap: 0.4rem;
    transition: color var(--duration-base) ease;
  }

  .external-link-icon {
    font-size: 0.9rem;
    opacity: 0;
    transform: translateY(2px) translateX(-4px);
    transition:
      opacity var(--duration-base) var(--ease-spring),
      transform var(--duration-base) var(--ease-spring);
    color: var(--color-accent);
  }

  a.tool-card:hover .external-link-icon {
    opacity: 1;
    transform: translateY(0) translateX(0);
  }

  .tool-card:hover .tool-title {
    color: var(--color-accent);
  }

  .tool-description {
    margin: 0;
    font-size: 0.9rem;
    line-height: 1.65;
    color: var(--color-muted);
    word-wrap: break-word;
    white-space: normal;
    transition: color var(--duration-base) ease;
  }

  a.tool-card:hover .tool-description {
    color: rgba(var(--text), 0.85);
  }

  .level-indicator {
    position: absolute;
    top: 1rem;
    right: 1rem;
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: var(--color-accent);
    opacity: 0.4;
  }

  .tool-card[data-level="mastered"] .level-indicator {
    opacity: 1;
    box-shadow: 0 0 8px rgba(var(--accent), 0.4);
  }
  .tool-card[data-level="practiced"] .level-indicator {
    opacity: 0.6;
  }
  .tool-card[data-level="exploring"] .level-indicator {
    background: var(--color-muted);
    opacity: 0.3;
  }

  @media (max-width: 640px) {
    .tool-card {
      padding: 1.25rem;
    }
    .tool-inner {
      gap: 1rem;
    }
    .tool-icon-container {
      width: 2.5rem;
      height: 2.5rem;
      min-width: 2.5rem;
      min-height: 2.5rem;
    }
    .tool-title {
      font-size: 1rem;
    }
    .tool-description {
      font-size: 0.85rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .tool-card:hover {
      transform: none;
    }
  }
</style>
