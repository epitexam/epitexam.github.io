---
interface Props {
    label: string;
    href?: string;
    linkText?: string;
    level?: "h1" | "h2" | "h3" | "h4";
    id?: string;
}

const { label, href, linkText, level = "h2", id } = Astro.props;
const HeadingTag = level;
---

<div class="section-header">
    <div class="section-badge">
        <span class="badge-dot" aria-hidden="true"></span>
        <HeadingTag class="badge-label" id={id}>{label}</HeadingTag>
    </div>
    <span class="section-line" aria-hidden="true"></span>
    {
        href && linkText && (
            <a href={href} class="section-link">
                {linkText}
                <svg
                    class="arrow-icon"
                    width="16"
                    height="16"
                    viewBox="0 0 16 16"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="1.8"
                    aria-hidden="true"
                >
                    <path
                        d="M6 12L10 8L6 4"
                        stroke-linecap="round"
                        stroke-linejoin="round"
                    />
                </svg>
            </a>
        )
    }
</div>

<style>
    .section-header {
        display: flex;
        align-items: center;
        gap: 1.25rem;
        width: 100%;
        padding-block: 1.5rem;
    }

    .section-badge {
        display: inline-flex;
        align-items: center;
        gap: 0.6rem;
        padding: 0.5rem 1.2rem;
        background: rgba(var(--accent), 0.05);
        border: 1px solid rgba(var(--accent), 0.15);
        border-radius: 999px;
        font-family: var(--font-title);
        font-size: 0.8rem;
        font-weight: 500;
        text-transform: uppercase;
        letter-spacing: 0.08em;
        color: var(--color-accent);
        white-space: nowrap;
    }

    .badge-label {
        margin: 0;
        padding: 0;
        font-size: inherit;
        font-weight: inherit;
        font-family: inherit;
        line-height: inherit;
        color: inherit;
        letter-spacing: inherit;
        text-transform: inherit;
    }

    .badge-dot {
        width: 8px;
        height: 8px;
        background: var(--color-accent);
        border-radius: 50%;
        flex-shrink: 0;
        animation: pulse 2.5s ease-in-out infinite;
    }

    .section-line {
        flex: 1;
        height: 1px;
        background: var(--color-border);
        opacity: 0.6;
    }

    .section-link {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        font-family: var(--font-title);
        font-size: 0.8rem;
        font-weight: 600;
        letter-spacing: 0.05em;
        color: var(--color-muted);
        text-decoration: none;
        white-space: nowrap;
        transition: color var(--duration-fast) ease;
    }

    .section-link:hover {
        color: var(--color-text);
    }

    .arrow-icon {
        width: 16px;
        height: 16px;
        flex-shrink: 0;
        transition: transform var(--duration-base) var(--ease-spring);
    }

    .section-link:hover .arrow-icon {
        transform: translateX(4px);
    }

    @keyframes pulse {
        0%,
        100% {
            opacity: 1;
        }
        50% {
            opacity: 0.4;
        }
    }

    /* ── 768px ── */
    @media (max-width: 768px) {
        .section-header {
            gap: 1rem;
        }

        .section-badge {
            padding: 0.45rem 1rem;
            font-size: 0.75rem;
            letter-spacing: 0.07em;
        }

        .section-link {
            font-size: 0.75rem;
        }
    }

    /* ── 600px ── */
    @media (max-width: 600px) {
        .section-header {
            gap: 0.65rem;
        }

        .section-badge {
            padding: 0.4rem 0.85rem;
            font-size: 0.68rem;
            gap: 0.45rem;
            letter-spacing: 0.05em;
        }

        .badge-dot {
            width: 6px;
            height: 6px;
        }

        .section-link {
            font-size: 0.68rem;
            gap: 0.35rem;
            letter-spacing: 0.03em;
        }

        .arrow-icon {
            width: 14px;
            height: 14px;
        }
    }

    /* ── 480px : badge + lien sur la même ligne, séparateur masqué ── */
    @media (max-width: 480px) {
        .section-header {
            justify-content: space-between;
            gap: 0.5rem;
        }

        .section-line {
            display: none;
        }

        .section-badge {
            padding: 0.35rem 0.75rem;
            font-size: 0.62rem;
            gap: 0.4rem;
            letter-spacing: 0.04em;
        }

        .badge-dot {
            width: 5px;
            height: 5px;
        }

        .section-link {
            font-size: 0.62rem;
            gap: 0.3rem;
        }

        .arrow-icon {
            width: 12px;
            height: 12px;
        }
    }

    /* ── 360px ── */
    @media (max-width: 360px) {
        .section-badge {
            padding: 0.3rem 0.65rem;
            font-size: 0.58rem;
        }

        .section-link {
            font-size: 0.58rem;
        }

        .arrow-icon {
            width: 11px;
            height: 11px;
        }
    }

    @media (prefers-reduced-motion: reduce) {
        .badge-dot {
            animation: none;
            opacity: 0.8;
        }

        .section-link:hover .arrow-icon {
            transform: none;
        }
    }
</style>