---
import BaseHead from "../components/BaseHead.astro";
import Footer from "../components/Footer.astro";
import Header from "../components/Header.astro";
import { SITE_TITLE } from "../consts";

const success = Astro.url.searchParams.get("success") === "true";
---

<!doctype html>
<html lang="fr">
    <head>
        <BaseHead
            title={`Contact - ${SITE_TITLE}`}
            description="Contactez-moi pour discuter de vos projets, collaborations ou questions."
        />
    </head>

    <body>
        <Header />

        <main class="contact-main">
            <div class="contact-wrapper">
                <header class="contact-header">
                    <h1 class="contact-title">Travaillons ensemble.</h1>
                    <p class="contact-intro">
                        Un projet, une question, ou juste envie d'échanger : je
                        réponds à tous les messages sous 48h.
                    </p>
                </header>

                {
                    success && (
                        <div
                            class="notification success-notification"
                            role="status"
                            aria-live="polite"
                        >
                            <svg
                                width="16"
                                height="16"
                                viewBox="0 0 24 24"
                                fill="none"
                                stroke="currentColor"
                                stroke-width="2.5"
                                aria-hidden="true"
                            >
                                <polyline points="20 6 9 17 4 12" />
                            </svg>
                            Message envoyé : je vous réponds dès que possible.
                        </div>
                    )
                }

                <div
                    id="errorNotification"
                    class="notification error-notification hidden"
                    role="alert"
                    aria-live="assertive"
                >
                </div>

                <form
                    id="contactForm"
                    action="https://formspree.io/f/xovgkrbn"
                    method="POST"
                    class="contact-form"
                    autocomplete="on"
                    novalidate
                >
                    <input
                        type="hidden"
                        name="_next"
                        value={`${Astro.url.origin}${Astro.url.pathname}?success=true`}
                    />
                    <input
                        type="hidden"
                        name="_subject"
                        value={`Nouveau message - ${SITE_TITLE}`}
                    />

                    <div class="honeypot" aria-hidden="true">
                        <input
                            type="text"
                            name="website"
                            tabindex="-1"
                            autocomplete="off"
                        />
                    </div>

                    <div class="field-group">
                        <label for="email" class="field-label"
                            >Votre email</label
                        >
                        <input
                            type="email"
                            id="email"
                            name="email"
                            required
                            placeholder="nom@domaine.com"
                            class="input-field"
                            autocomplete="email"
                        />
                        <p class="field-hint">Ne sera jamais partagé.</p>
                    </div>

                    <div class="field-group">
                        <label for="message" class="field-label"
                            >Votre message</label
                        >
                        <textarea
                            id="message"
                            name="message"
                            required
                            rows="6"
                            placeholder="Décrivez votre projet, votre question…"
                            class="input-field textarea-field"
                            minlength="20"
                            maxlength="2000"
                            spellcheck="true"></textarea>
                        <div class="textarea-meta">
                            <span class="field-hint">Markdown supporté.</span>
                            <span class="char-counter">
                                <span id="charCount">0</span><span
                                    class="char-max">/2000</span
                                >
                            </span>
                        </div>
                    </div>

                    <div class="consent-row">
                        <input
                            type="checkbox"
                            id="privacy"
                            name="privacy"
                            required
                            class="consent-checkbox"
                        />
                        <label for="privacy" class="consent-label">
                            J'accepte que mes données soient utilisées
                            uniquement pour répondre à mon message.
                        </label>
                    </div>

                    <button id="submitBtn" type="submit" class="submit-btn">
                        <span id="submitLabel">Envoyer</span>
                        <svg
                            id="submitArrow"
                            width="16"
                            height="16"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            aria-hidden="true"
                        >
                            <line x1="5" y1="12" x2="19" y2="12"></line>
                            <polyline points="12 5 19 12 12 19"></polyline>
                        </svg>
                        <svg
                            id="submitSpinner"
                            class="spinner hidden"
                            width="16"
                            height="16"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                            aria-hidden="true"
                        >
                            <path d="M21 12a9 9 0 1 1-6.219-8.56"></path>
                        </svg>
                    </button>
                </form>
            </div>
        </main>

        <Footer />
    </body>
</html>

<style>
    .contact-main {
        --warn-rgb: 220, 140, 40;
    }

    .contact-main {
        justify-content: center;
        align-items: center;
    }

    .contact-main :is(h1, h2, h3, h4, h5, h6) {
        animation: none !important;
    }

    .contact-wrapper {
        width: 100%;
        max-width: 44rem;
        display: flex;
        flex-direction: column;
        gap: 1.75rem;
    }

    .contact-header {
        display: flex;
        flex-direction: column;
        gap: 0.75rem;
    }

    .contact-title {
        font-size: clamp(1.75rem, 5vw, 2.5rem);
        font-weight: 700;
        line-height: 1.1;
        letter-spacing: -0.03em;
        margin: 0;
    }

    .contact-intro {
        font-size: 0.95rem;
        line-height: 1.75;
        color: var(--color-muted);
        margin: 0;
    }

    .notification {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        padding: 0.9rem 1.1rem;
        border-radius: var(--radius-md);
        font-size: 0.875rem;
        font-weight: 500;
        animation: slideDown var(--duration-base) var(--ease-spring) both;
    }

    .success-notification {
        background: rgba(var(--accent), 0.08);
        border: 1px solid rgba(var(--accent), 0.28);
        color: var(--color-accent);
    }

    .error-notification {
        background: rgba(var(--color-error-rgb), 0.08);
        border: 1px solid rgba(var(--color-error-rgb), 0.28);
        color: rgb(var(--color-error-rgb));
    }

    .notification.hidden {
        display: none !important;
    }

    @keyframes slideDown {
        from {
            opacity: 0;
            transform: translateY(-8px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .contact-form {
        display: flex;
        flex-direction: column;
        gap: 1.75rem;
        background: linear-gradient(
            145deg,
            rgb(var(--bg)) 0%,
            rgba(var(--accent), 0.04) 100%
        );
        border: 1px solid var(--color-border);
        border-radius: var(--radius-md);
        padding: 2rem;
        box-shadow:
            var(--shadow-soft),
            inset 0 1px 0 var(--tint-05);
    }

    .honeypot {
        display: none;
    }

    .field-group {
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
    }

    .field-label {
        font-family: var(--font-title);
        font-size: 0.75rem;
        text-transform: uppercase;
        letter-spacing: 0.06em;
        color: var(--color-text);
    }

    .input-field {
        width: 100%;
        background: rgba(var(--accent), 0.03);
        border: 1px solid rgba(var(--accent), 0.15);
        border-radius: var(--radius-sm);
        padding: 0.75rem 1rem;
        color: var(--color-text);
        font-family: var(--font-body);
        font-size: 0.95rem;
        line-height: 1.5;
        transition:
            border-color var(--duration-fast) ease,
            background var(--duration-fast) ease,
            box-shadow var(--duration-fast) ease;
    }

    .input-field::placeholder {
        color: rgba(var(--text), 0.22);
    }

    .input-field:focus {
        outline: none;
        border-color: rgba(var(--accent), 0.55);
        background: rgba(var(--accent), 0.05);
        box-shadow: 0 0 0 3px rgba(var(--accent), 0.1);
    }

    .textarea-field {
        resize: vertical;
        min-height: 140px;
    }

    .field-hint {
        font-size: 0.7rem;
        color: var(--color-muted);
        margin: 0;
    }

    .textarea-meta {
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .char-counter {
        font-family: var(--font-code);
        font-size: 0.68rem;
        color: var(--color-muted);
        transition: color var(--duration-fast) ease;
    }

    .char-max {
        opacity: 0.5;
    }

    .char-counter.near-limit {
        color: rgb(var(--warn-rgb));
    }
    .char-counter.at-limit {
        color: rgb(var(--color-error-rgb));
    }

    .consent-row {
        display: flex;
        align-items: flex-start;
        gap: 0.75rem;
        padding: 0.85rem 1rem;
        background: rgba(var(--accent), 0.03);
        border: 1px solid rgba(var(--accent), 0.1);
        border-radius: var(--radius-sm);
    }

    .consent-checkbox {
        margin-top: 0.1rem;
        accent-color: var(--color-accent);
        flex-shrink: 0;
        cursor: pointer;
        width: 15px;
        height: 15px;
    }

    .consent-label {
        font-size: 0.78rem;
        color: var(--color-muted);
        cursor: pointer;
        line-height: 1.6;
    }

    .submit-btn {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 0.6rem;
        padding: 0.85rem 1.5rem;
        background: var(--color-accent);
        color: var(--color-bg);
        border: none;
        border-radius: var(--radius-sm);
        font-family: var(--font-title);
        font-size: 0.8rem;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.1em;
        cursor: pointer;
        transition:
            transform var(--duration-base) var(--ease-spring),
            box-shadow var(--duration-base) ease,
            filter var(--duration-fast) ease;
    }

    .submit-btn:hover:not(:disabled) {
        transform: translateY(-2px);
        box-shadow: 0 6px 20px rgba(var(--accent), 0.35);
        filter: brightness(1.06);
    }

    .submit-btn:active:not(:disabled) {
        transform: translateY(0);
    }
    .submit-btn:disabled {
        opacity: 0.55;
        cursor: not-allowed;
    }

    .spinner {
        animation: spin 0.8s linear infinite;
    }

    @keyframes spin {
        from {
            transform: rotate(0deg);
        }
        to {
            transform: rotate(360deg);
        }
    }

    .hidden {
        display: none !important;
    }

    @media (max-width: 768px) {
        .contact-main {
            align-items: flex-start;
            justify-content: flex-start;
        }
        .contact-form {
            padding: 1.5rem;
            gap: 1.5rem;
        }
    }

    @media (max-width: 480px) {
        .contact-wrapper {
            gap: 1.25rem;
        }
        .contact-form {
            padding: 1.1rem;
            gap: 1.25rem;
        }
        .contact-title {
            font-size: 1.6rem;
        }
        .submit-btn {
            width: 100%;
        }
    }

    @media (prefers-reduced-motion: reduce) {
        .spinner {
            animation: none;
        }
        .notification {
            animation: none;
        }
        .submit-btn:hover:not(:disabled) {
            transform: none;
        }
    }
</style>

<script>
    (function () {
        const form = document.getElementById(
            "contactForm",
        ) as HTMLFormElement | null;
        const textarea = document.getElementById(
            "message",
        ) as HTMLTextAreaElement | null;
        const charCountEl = document.getElementById("charCount");
        const charCounter = document.querySelector(".char-counter");
        const submitBtn = document.getElementById(
            "submitBtn",
        ) as HTMLButtonElement | null;
        const submitLabel = document.getElementById("submitLabel");
        const submitArrow = document.getElementById("submitArrow");
        const submitSpinner = document.getElementById("submitSpinner");
        const errorNotif = document.getElementById("errorNotification");

        if (!form || !textarea) return;

        const updateCount = () => {
            const len = textarea.value.length;
            if (charCountEl) charCountEl.textContent = String(len);
            if (charCounter) {
                charCounter.classList.toggle(
                    "near-limit",
                    len > 1800 && len < 2000,
                );
                charCounter.classList.toggle("at-limit", len >= 2000);
            }
        };

        textarea.addEventListener("input", updateCount);
        updateCount();

        const showError = (msg: string) => {
            if (!errorNotif) return;
            errorNotif.textContent = msg;
            errorNotif.classList.remove("hidden");
            errorNotif.scrollIntoView({ behavior: "smooth", block: "nearest" });
            setTimeout(() => errorNotif.classList.add("hidden"), 5000);
        };

        form.addEventListener("submit", (e) => {
            const honeypot = form.querySelector<HTMLInputElement>(
                'input[name="website"]',
            );
            if (honeypot?.value.trim()) {
                e.preventDefault();
                return;
            }

            const privacy = form.querySelector<HTMLInputElement>(
                'input[name="privacy"]',
            );
            if (!privacy?.checked) {
                e.preventDefault();
                showError("Veuillez accepter la politique de confidentialité.");
                privacy?.focus();
                return;
            }

            if (textarea.value.trim().length < 20) {
                e.preventDefault();
                showError("Le message doit contenir au moins 20 caractères.");
                textarea.focus();
                return;
            }

            if (submitBtn && submitLabel && submitArrow && submitSpinner) {
                submitBtn.disabled = true;
                submitLabel.textContent = "Envoi en cours…";
                submitArrow.classList.add("hidden");
                submitSpinner.classList.remove("hidden");
            }
        });
    })();
</script>