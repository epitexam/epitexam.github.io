---
import BaseHead from "../components/BaseHead.astro";
import Footer from "../components/Footer.astro";
import Header from "../components/Header.astro";
import { SITE_DESCRIPTION, SITE_TITLE } from "../consts";
---

<!doctype html>
<html lang="fr">
    <head>
        <BaseHead
            title={`Contact — ${SITE_TITLE}`}
            description="Me contacter pour discuter, collaborer ou réagir à un article."
        />
    </head>

    <body
        class="flex flex-col min-h-screen bg-[rgb(var(--bg))] text-[rgb(var(--text))] font-mono"
    >
        <Header />

        <main class="animate-fadeIn">
            <section class="max-w-2xl">
                <h1 class="glow-text">Contact</h1>

                <p>
                    Une question sur un projet, une réaction à un article du
                    blog, ou simplement envie d’échanger autour du développement
                    et de la création ? N’hésitez pas à me laisser un message.
                </p>

                <p>Je lis tout et je réponds dès que possible.</p>
            </section>

            <form
                action="https://formspree.io/f/xovgkrbn"
                method="POST"
                class="mt-10 flex flex-col gap-6 max-w-xl"
            >
                <label class="flex flex-col gap-2">
                    <span class="text-[rgb(var(--accent))] font-semibold"
                        >Email</span
                    >
                    <input
                        type="email"
                        name="email"
                        required
                        placeholder="email@example.com"
                        class="px-4 py-2 bg-black/20 border border-[rgba(0,255,150,0.25)] rounded-md
                   focus:outline-none focus:border-[rgb(var(--accent))]
                   focus:shadow-[var(--glow)] placeholder:text-white/40"
                    />
                </label>

                <label class="flex flex-col gap-2">
                    <span class="text-[rgb(var(--accent))] font-semibold"
                        >Sujet</span
                    >
                    <input
                        type="text"
                        name="sujet"
                        placeholder="Projet, collaboration, question…"
                        class="px-4 py-2 bg-black/20 border border-[rgba(0,255,150,0.25)] rounded-md
                   focus:outline-none focus:border-[rgb(var(--accent))]
                   focus:shadow-[var(--glow)] placeholder:text-white/40"
                    />
                </label>

                <label class="flex flex-col gap-2">
                    <span class="text-[rgb(var(--accent))] font-semibold"
                        >Message</span
                    >
                    <textarea
                        name="message"
                        rows="6"
                        required
                        placeholder="Écrivez votre message ici..."
                        class="px-4 py-2 bg-black/20 border border-[rgba(0,255,150,0.25)] rounded-md
                   focus:outline-none focus:border-[rgb(var(--accent))]
                   focus:shadow-[var(--glow)] placeholder:text-white/40"
                    ></textarea>
                </label>


                <input
                    type="text"
                    name="_gotcha"
                    style="display:none"
                    tabindex="-1"
                    autocomplete="off"
                />

                <button
                    type="submit"
                    class="mt-4 px-6 py-3 border border-[rgb(var(--accent))] text-[rgb(var(--accent))]
                 font-semibold rounded-md transition
                 hover:bg-[rgba(0,255,150,0.1)] hover:shadow-[var(--glow)]"
                >
                    Envoyer
                </button>
            </form>
        </main>

        <Footer />
    </body>
</html>
